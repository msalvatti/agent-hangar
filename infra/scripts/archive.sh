#!/usr/bin/env bash
# archive.sh — tears down the instance (compose down -v, reap workspace containers); placeholder until the infra scripts lane implements it.
set -euo pipefail
echo "not implemented yet (lane W1-I)" >&2
exit 1
