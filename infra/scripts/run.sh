#!/usr/bin/env bash
# run.sh — starts web + worker on the instance's ports (placeholder until the infra scripts lane implements it).
set -euo pipefail
echo "not implemented yet (lane W1-I)" >&2
exit 1
