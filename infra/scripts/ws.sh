#!/usr/bin/env bash
# ws.sh list|reap — lists or destroys the workspace containers of this instance; placeholder until the infra scripts lane implements it.
set -euo pipefail
echo "not implemented yet (lane W1-I)" >&2
exit 1
