#!/usr/bin/env bash
# db-prune.sh — drops Workspace rows of destroyed containers older than 30 days; placeholder until the infra scripts lane implements it.
set -euo pipefail
echo "not implemented yet (lane W1-I)" >&2
exit 1
