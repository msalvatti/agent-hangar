{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "description": "BullMQ consumers, scheduler reconciliation and workspace garbage collection.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -b",
    "start": "node dist/main.js",
    "lint": "eslint .",
    "typecheck": "tsc -b",
    "test": "vitest run"
  }
}
