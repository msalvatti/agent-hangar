{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "description": "BullMQ consumers, scheduler reconciliation and workspace garbage collection.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --conditions=development src/main.ts",
    "build": "bash ../../scripts/tsc-build.sh",
    "start": "node dist/main.js",
    "lint": "eslint .",
    "typecheck": "bash ../../scripts/tsc-build.sh",
    "test": "vitest run",
    "test:mutation": "stryker run",
    "test:mutation:scoped": "stryker run --incrementalFile .stryker-tmp/inc-scoped.json --mutate",
    "test:integration": "DOCKER_AVAILABLE=1 vitest run --project integration --coverage.enabled=false"
  },
  "dependencies": {
    "@agent-hangar/core": "workspace:*",
    "bullmq": "^6.1.2",
    "ioredis": "^6.0.0",
    "pino": "^10.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.3",
    "tsx": "^4.23.12"
  }
}
