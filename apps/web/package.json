{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "description": "Next.js App Router application: UI, API routes and SSE.",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:e2e": "playwright test"
  }
}
